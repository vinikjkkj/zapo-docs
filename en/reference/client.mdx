---
title: "WaClient & coordinators"
description: "The client surface and a method index for every coordinator."
icon: "code"
---

## WaClient

```ts
new WaClient(options: WaClientOptions, logger?: Logger)
```

See [Configuration](/en/concepts/configuration) for every option.

### Lifecycle & state

| Member | Signature | Description |
| --- | --- | --- |
| `connect` | `() => Promise<void>` | Open the socket; drives pairing on first run. |
| `disconnect` | `() => Promise<void>` | Close gracefully, keep credentials. |
| `logout` | `(reason?) => Promise<void>` | Unlink the device server-side. |
| `getState` | `() => WaAuthState` | Current auth/connection state. |
| `getCredentials` | `() => WaAuthCredentials \| null` | Current credentials, if paired. |
| `getClockSkewMs` | `() => number \| null` | Server clock skew estimate. |

### Event emitter

| Member | Signature |
| --- | --- |
| `on` | `(event, listener) => this` |
| `once` | `(event, listener) => this` |
| `off` | `(event, listener) => this` |

All three are typed against [`WaClientEventMap`](/en/concepts/events).

### Coordinator getters

| Getter | Type |
| --- | --- |
| `auth` | `WaAuthClient` |
| `message` | `WaMessageCoordinator` |
| `presence` | `WaPresenceCoordinator` |
| `chat` | `WaAppStateMutationCoordinator` |
| `group` | `WaGroupCoordinator` |
| `status` | `WaStatusCoordinator` |
| `broadcastList` | `WaBroadcastListCoordinator` |
| `newsletter` | `WaNewsletterCoordinator` |
| `privacy` | `WaPrivacyCoordinator` |
| `profile` | `WaProfileCoordinator` |
| `business` | `WaBusinessCoordinator` |
| `bot` | `WaBotCoordinator` |
| `email` | `WaEmailCoordinator` |
| `lowlevel` | `WaLowLevelCoordinator` |

---

## message

`WaMessageCoordinator` — [guide](/en/guides/sending-messages).

| Method | Signature |
| --- | --- |
| `send` | `(to, content: WaSendMessageContent, options?) => Promise<WaMessagePublishResult>` |
| `sendReceipt` | `(event \| events, options?)` / `(jid, ids, options?) => Promise<void>` |
| `download` | `(source, options?) => Promise<Readable>` |
| `downloadToFile` | `(source, filePath, options?) => Promise<void>` |
| `downloadBytes` | `(source, options?) => Promise<Uint8Array>` |
| `tryDecryptAddon` | `(event) => Promise<void>` |
| `syncSignalSession` | `(jid, reasonIdentity?) => Promise<void>` |

---

## group

`WaGroupCoordinator` — [guide](/en/guides/groups).

| Method | Signature |
| --- | --- |
| `queryGroupMetadata` | `(groupJid) => Promise<WaGroupMetadata>` |
| `queryAllGroups` | `() => Promise<readonly WaGroupMetadata[]>` |
| `queryGroupInviteInfo` | `(code) => Promise<BinaryNode>` |
| `createGroup` | `(subject, participants, options?) => Promise<BinaryNode>` |
| `setSubject` | `(groupJid, subject) => Promise<void>` |
| `setDescription` | `(groupJid, description \| null, prevDescId?) => Promise<void>` |
| `setSetting` | `(groupJid, setting, enabled) => Promise<void>` |
| `addParticipants` / `removeParticipants` | `(groupJid, jids) => Promise<BinaryNode>` |
| `promoteParticipants` / `demoteParticipants` | `(groupJid, jids) => Promise<BinaryNode>` |
| `leaveGroup` | `(groupJids) => Promise<BinaryNode>` |
| `revokeInvite` | `(groupJid) => Promise<BinaryNode>` |
| `joinGroupViaInvite` | `(code) => Promise<BinaryNode>` |
| `createCommunity` | `(subject, options?) => Promise<WaGroupMetadata>` |
| `linkSubGroups` / `unlinkSubGroups` | `(communityJid, jids, options?) => Promise<…>` |
| `fetchSubGroups` | `(communityJid) => Promise<WaCommunitySubGroupsResult>` |
| `queryMembershipApprovalRequests` | `(groupJid) => Promise<readonly WaMembershipRequest[]>` |
| `approveMembershipRequests` / `rejectMembershipRequests` / `cancelMembershipRequests` | `(groupJid, jids) => Promise<void>` |

---

## newsletter

`WaNewsletterCoordinator` — [guide](/en/guides/newsletters). Combines discovery, admin, and messaging ops.

| Method | Signature |
| --- | --- |
| `fetch` / `fetchByInvite` | `(jid \| code, options?) => Promise<WaNewsletterMetadata>` |
| `listSubscribed` | `(options?) => Promise<readonly WaNewsletterMetadata[]>` |
| `searchDirectory` / `fetchRecommended` / `fetchSimilar` | `(options?) => Promise<…>` |
| `follow` / `unfollow` | `(jid) => Promise<void>` |
| `mute` | `(input) => Promise<void>` |
| `send` | `(jid, content, options?) => Promise<WaNewsletterSendResult>` |
| `editMessage` | `(jid, parentMessageId, content) => Promise<WaNewsletterSendResult>` |
| `react` / `revoke` / `votePoll` / `sendViewReceipt` | `(input) => Promise<{ stanzaId }>` |
| `fetchMessages` / `fetchMessageUpdates` | `(input) => Promise<BinaryNode>` |
| `create` | `(input) => Promise<WaNewsletterMetadata>` |
| `update` | `(jid, input) => Promise<WaNewsletterMetadata>` |
| `delete` | `(jid) => Promise<void>` |
| `fetchAdminInfo` / `fetchFollowers` / `fetchInsights` | `(jid, …) => Promise<…>` |
| `createAdminInvite` / `changeOwner` / `demoteAdmin` | `(input) => Promise<…>` |

---

## privacy

`WaPrivacyCoordinator` — [guide](/en/guides/profile-privacy#privacy).

| Method | Signature |
| --- | --- |
| `getPrivacySettings` | `() => Promise<WaPrivacySettings>` |
| `setPrivacySetting` | `(setting, value) => Promise<void>` |
| `getBlocklist` | `() => Promise<WaBlocklistResult>` |
| `blockUser` / `unblockUser` | `(jid) => Promise<void>` |
| `getDisallowedList` | `(category) => Promise<WaPrivacyDisallowedListResult>` |

---

## profile

`WaProfileCoordinator` — [guide](/en/guides/profile-privacy#profile).

| Method | Signature |
| --- | --- |
| `getProfilePicture` | `(jid, type?, existingId?) => Promise<WaProfilePictureResult>` |
| `setProfilePicture` | `(imageBytes, targetJid?) => Promise<string \| null>` |
| `deleteProfilePicture` | `(targetJid?) => Promise<void>` |
| `getStatus` / `setStatus` | `(jid) / (text) => Promise<…>` |
| `getProfiles` | `(jids) => Promise<readonly WaProfileInfo[]>` |
| `getOwnUsername` | `() => Promise<WaOwnUsernameResult>` |
| `setUsername` / `deleteUsername` | `(input) / () => Promise<boolean>` |
| `checkUsernameAvailability` | `(username) => Promise<…>` |

---

## status

`WaStatusCoordinator` — [guide](/en/guides/presence-status#status-broadcasts).

| Method | Signature |
| --- | --- |
| `send` | `(input: WaSendStatusInput) => Promise<WaMessagePublishResult>` |
| `setPrivacy` | `(input) => Promise<void>` |
| `setUserMuted` | `(jid, muted) => Promise<void>` |
| `revokeStatus` | `(input) => Promise<WaMessagePublishResult>` |

---

## business

`WaBusinessCoordinator` — [guide](/en/guides/profile-privacy#business).

| Method | Signature |
| --- | --- |
| `getBusinessProfile` | `(jids) => Promise<readonly WaBusinessProfileResult[]>` |
| `editBusinessProfile` | `(input) => Promise<void>` |
| `getVerifiedName` / `getVerifiedNames` | `(jid \| jids) => Promise<…>` |
| `updateCoverPhoto` / `deleteCoverPhoto` | `(media) / (id) => Promise<void>` |

---

## chat

`WaAppStateMutationCoordinator` — [guide](/en/guides/profile-privacy#chat-settings).

| Method | Signature |
| --- | --- |
| `setChatMute` | `(chatJid, muted, muteEndTimestampMs?) => Promise<void>` |
| `setChatPin` / `setChatArchive` / `setChatRead` / `setChatLock` | `(chatJid, boolean) => Promise<void>` |
| `setMessageStar` | `(message, starred) => Promise<void>` |
| `clearChat` / `deleteChat` | `(chatJid, options?) => Promise<void>` |
| `deleteMessageForMe` | `(message, options?) => Promise<void>` |
| `sync` | `(options?) => Promise<WaAppStateSyncResult>` |

---

## presence

`WaPresenceCoordinator` — [guide](/en/guides/presence-status).

| Method | Signature |
| --- | --- |
| `send` | `(type?: 'available' \| 'unavailable') => Promise<void>` |
| `sendChatstate` | `(jid, options) => Promise<void>` |
| `subscribe` | `(jid, options?) => Promise<void>` |

---

## bot, email, broadcastList

| Coordinator | Key methods |
| --- | --- |
| `WaBotCoordinator` | `listBots()`, `getBotProfile(jid, options?)`, `sendPrompt(to, content, options?)` |
| `WaEmailCoordinator` | `getStatus()`, `setEmail(email, context?)`, `requestVerificationCode(input)`, `verifyCode(code)`, `confirm(context?)` |
| `WaBroadcastListCoordinator` | `setList(input)`, `removeList(id)`, `send(input)` |
